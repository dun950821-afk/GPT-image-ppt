interface:
  display_name: "GPT Image PPT"
  short_description: "Rebuild raster slides with strict editable-deck QA"
  default_prompt: "Use $gpt-image-ppt to rebuild raster slides into a source-faithful editable PowerPoint deck and pass the mandatory delivery gates."
